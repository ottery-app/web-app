export const API_ENV = {
    /**
     * this is the time between query requests
     * @type {Number}
     */
    query_delta: 5000,

    /**
     * how long before the request times out
     * @type {Number}
     */
    timeout: 10000,
};