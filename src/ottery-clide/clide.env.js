export const CLIDE_ENV = {
    /**
     * this is the time between query requests
     * @type {Number}
     */
    query_delta: 5000,

    /**
     * this is the time it takes before killing a request
     */
    timeout: 10000,
};